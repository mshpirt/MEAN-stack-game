// public/js/controllers/MainCtrl.js
angular.module('MainCtrl', ['sessionService']).controller('MainController', function($scope, sessionData) {

    $scope.tagline = 'This line generated by MainCtrl.js. This page retrieves the express session generated by visiting the site. If a name is entered in New Game, it is associated with the session and remembered.';

    $scope.status;
    $scope.session;
    getSession();

    function getSession() 
    	{
        sessionData.getSession()
        .success(function (sessionResults) { $scope.session = "Welcome back, " + sessionResults; })
        .error(function (error) { $scope.status = 'Unable to retrieve session data: ' + error.message; });
        }

});